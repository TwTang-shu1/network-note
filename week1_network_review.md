# 计算机网络周复盘

## 本周学到的主线

### 1. 第1章：网络基础
- 计算机网络是把分散的主机通过通信系统连接起来，实现数据交换。
- 五层模型：应用层、运输层、网络层、数据链路层、物理层。
- 协议是规则，服务是下层给上层提供的能力，接口是上下层交接的位置。
- 四种时延：发送时延、传播时延、处理时延、排队时延。
- 封装方向：数据 -> 段 -> 包/数据报 -> 帧 -> 比特流。

### 2. 第3章前半：数据链路层
- 数据链路层负责相邻结点之间的传输。
- MAC 是数据链路层地址，IP 是网络层地址。
- ARP 的作用是把 IP 解析成 MAC。
- PPP 是点对点协议，适合一对一链路。
- 广播信道下要做介质访问控制，典型机制是 CSMA/CD。
- 以太网里传的是帧，交换机按 MAC 转发。

### 3. 运输层与应用层
- TCP 面向连接，可靠、有序传输。
- UDP 无连接，尽力而为，速度快。
- 三次握手用于建立连接，四次挥手用于关闭连接。
- 端口用于区分同一台主机上的不同进程。
- DNS 负责把域名解析成 IP。
- HTTP 是明文网页协议，HTTPS 是加密后的 HTTP，默认端口分别是 80 和 443。

## 本周实操记录

### 1. 查看网卡、IP、路由
```bash
ip addr show ens33
ip route
```

观察到：
- 网卡名：`ens33`
- MAC 地址：`00:0c:29:76:ac:51`
- IP 地址：`192.168.108.128/24`
- 默认网关：`192.168.108.2`

### 2. 测试连通性
```bash
ping -c 4 192.168.108.2
```

结论：
- 网关可达
- 延迟很低，局域网链路正常

### 3. 查看邻居缓存
```bash
ip neigh
```

看到过的状态：
- `INCOMPLETE`：ARP 还没查到 MAC
- `DELAY`：已经有记录，但还在确认是否可达

### 4. 抓 ARP 包
```bash
sudo ip neigh flush 192.168.108.2
sudo tcpdump -n -e arp or icmp
ping -c 1 192.168.108.2
```

观察到：
- ARP Request 是广播
- ARP Reply 返回网关 MAC
- `ping` 成功前会先发生 ARP 解析

### 5. 查看监听端口
```bash
ss -tuln
```

理解到：
- `0.0.0.0:631` 表示监听所有 IPv4 地址
- `127.0.0.1:631` 表示只允许本机访问
- 端口用于区分同一台主机上的不同服务

## 这周踩过的坑

- `ping -c 192.168.108.2` 写错了，`-c` 后面要跟次数。
- `ip route` 一度没有输出，原因是 VMware NAT 服务没正常工作。
- `NO-CARRIER` 说明虚拟网卡没有真正连上。
- `ip neigh` 里有缓存，不一定每次都会重新发 ARP。

## 解决过的问题

- 重启了 Windows 宿主机上的 VMware NAT Service。
- 恢复了虚拟机的 NAT 网络。
- 让虚拟机重新拿到可用的 IP 和默认网关。

## 下周继续学什么

- TCP 三次握手和四次挥手状态机
- TCP 可靠传输、流量控制、拥塞控制
- UDP 的使用场景
- HTTP 请求/响应、状态码、Cookie、Session

## 一句话总结

这周已经把“IP、MAC、ARP、网关、端口、TCP/UDP、DNS、HTTP/HTTPS”这条主线串起来了，后面就是继续把运输层和应用层的细节补齐。
