# 第一周20条指令

1. `ip addr` - 查看网卡、IP、MAC
2. `ip route` - 查看路由和默认网关
3. `ip neigh` - 查看邻居缓存
4. `ip link` - 查看链路层状态
5. `hostname -I` - 快速查看本机IP
6. `ping -c 4 192.168.108.2` - 测试网关连通性
7. `ping -c 4 8.8.8.8` - 测试公网IP连通性
8. `ping baidu.com` - 测试DNS和连通性
9. `ping -c 4 baidu.com` - 测试域名连通性
10. `traceroute 8.8.8.8` - 查看经过的跳数
11. `nslookup baidu.com` - 查看域名解析结果
12. `dig baidu.com` - 查看更详细的DNS信息
13. `curl -I http://baidu.com` - 查看HTTP响应头
14. `curl -I https://baidu.com` - 查看HTTPS响应头
15. `curl -I http://www.baidu.com` - 查看HTTP主页响应头
16. `curl -I https://www.baidu.com` - 查看HTTPS主页响应头
17. `curl -s https://ifconfig.me` - 查看公网出口IP
18. `ss -tuln` - 查看监听端口
19. `ss -tulnp` - 查看监听端口和进程
20. `netstat -tuln` - 查看监听端口（对照用）

## 补充
- `whoami`：查看当前用户
- `uname -a`：查看系统和内核信息
- `cat /etc/resolv.conf`：查看DNS配置
- `arp -a`：查看ARP缓存
- `sudo ip neigh flush 192.168.108.2`：清理邻居缓存
- `sudo tcpdump -n -e arp or icmp`：抓ARP和ICMP包
